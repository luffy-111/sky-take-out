<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">

    <!--新增套餐-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal (category_id, name, price, status,
                             description, image, create_time, update_time, create_user, update_user)
        values (#{categoryId}, #{name}, #{price}, #{status},
                #{description}, #{image}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <!--分页查询套餐-->
    <select id="pageQuery" resultType="com.sky.entity.Setmeal">
        select setmeal.id          as id,
               setmeal.category_id as categoryId,
               setmeal.name        as name,
               setmeal.price       as price,
               setmeal.status      as status,
               setmeal.description as description,
               setmeal.image       as image,
               setmeal.update_time as updateTime,
               category.name       as categoryName
        from setmeal
                 left join category on setmeal.category_id = category.id
    </select>

    <select id="getById" resultType="com.sky.entity.Setmeal">
        select setmeal.id          as id,
               setmeal.category_id as categoryId,
               setmeal.name        as name,
               setmeal.price       as price,
               setmeal.status      as status,
               setmeal.description as description,
               setmeal.image       as image,
               setmeal.update_time as updateTime,
               category.name       as categoryName
        from setmeal
                 left join category on setmeal.category_id = category.id
        where setmeal.id = #{id}
    </select>

    <delete id="deleteById">
        delete from setmeal where id in
        <foreach collection="ids" item="id" separator="," close=")" open="(">
            #{id}
        </foreach>
    </delete>

    <select id="getByIdWithDish" resultType="com.sky.entity.Setmeal">
        select setmeal.category_id as categoryId,
               category.name       as categoryName,
               setmeal.description as description,
               setmeal.id          as id,
               setmeal.image       as image,
               setmeal.name        as name,
               setmeal.price       as price,
               setmeal.status      as status,
               setmeal.update_time as updateTime
        from setmeal
                 left join category on setmeal.category_id = category.id
        where setmeal.id = #{id}
    </select>

    <update id="update">
        update setmeal
        <set>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser}
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="list" parameterType="com.sky.entity.Setmeal" resultType="com.sky.entity.Setmeal">
        select * from setmeal
        <where>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from setmeal
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
        </where>
    </select>
</mapper>
